import React from 'react';

const validation = (props) => {
    render (
        <div>
            <p>
                Yo this text is too short
            </p>
        </div>
    );
};

export default validation;